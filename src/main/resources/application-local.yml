# Local profile: datasource values inherit from application.yml (env vars).
# Only override what actually differs on a developer laptop.
logging:
  level:
    com.clarityhub: DEBUG
    org.flywaydb: INFO