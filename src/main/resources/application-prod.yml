# Prod profile: datasource values inherit from application.yml (env vars).
# API docs are never exposed in production — attackers get no free API map.
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

logging:
  level:
    root: WARN
    com.clarityhub: INFO